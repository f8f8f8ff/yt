{{define "main"}}
	<p><a href="/">home</a></p>
	{{range .Files}}
		<a href="{{.Name}}">{{.Name}}</a> ({{.Size}})
		{{if eq true .Yt}}
			<a href="{{.YtLink}}">[yt]</a>
		{{end}}
		<br>
	{{end}}
{{end}}
